# The probe (probe/) is deliberately stdlib-only so CI needs no install.
# The engine reads dated YAML config, which needs a parser.
PyYAML>=6.0

# The contract tests validate contracts/fixtures/*.json against
# contracts/screens.schema.json. 4.18 is the floor for draft 2020-12 handled
# through the `referencing` resolver rather than the removed RefResolver.
jsonschema>=4.18

# The point-in-time store. DuckDB locally, Postgres in migration.
duckdb>=1.0
